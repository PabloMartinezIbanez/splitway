name: splitway_core
description: Core domain models and tracking engine for Splitway. Pure Dart, no Flutter.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
